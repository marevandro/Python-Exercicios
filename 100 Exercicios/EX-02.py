nome = input("Digite seu nome: ") #Lembrando que input sempre deve seguir quando precisamos da entrada do usuário.
print ("Bom dia", nome,"!") # Seguindo a logica, deixei o nome fora das aspas duplas e deu certo.
print ("É um prazer te conhecer {}".format(nome)) 
'''Primeiramente, o método format() em python serve basicamente para criar uma string que contém campos entre 
chaves que são substituidos pelos argumentos de format. ... Vale ressaltar que a numeração dos parâmetros
na string inicia pelo numero zero para o primeiro parâmetro, 1 para o segundo parâmetro e assim por diante.'''

